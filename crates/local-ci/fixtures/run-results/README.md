# Run result fixtures

Normalized `run-result.json` contracts. Checks keep schema version, workflow/job names, statuses, failing step, and step names while omitting volatile timestamps, durations, SHAs, worktree paths, and log paths.
