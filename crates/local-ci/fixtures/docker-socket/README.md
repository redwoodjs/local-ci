# Docker socket fixtures

Docker socket resolution contracts shared by TypeScript and Rust. Inputs capture environment variables and probed paths; expected output includes the socket URI and bind mount path, or expected error substrings.
