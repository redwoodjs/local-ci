# @redwoodjs/local-ci-darwin-x64

Reserved workspace package for the future macOS x64 native `local-ci` binary.

Native npm platform-package publishing is deferred. This package stays private until the release workflow builds, stages, and verifies a real `bin/local-ci` binary for this target.
